// src/engine/audio/AudioSystem.jsx
import { useEffect, useRef } from 'react';
import { audio } from './audio'; 

export const AudioSystem = ({ G }) => {
  // Audio effects have been stripped. 
  // This component now serves as a placeholder or can be extended for future speech-only events.
  return null; 
};